feat(dashboard): a brief that can't lie + AI motion layer + light-mode surfaces (declutter PR 2+3)

PR 2 - truthful brief, one voice:
- NEW lib/dashboard/brief-guard.ts (pure, tested): the AI daily brief now
  stores a queue fingerprint (open/overdue) in daily_briefs.sections.state;
  at read time getDashboardData compares it to the live radar. Stale (older
  prompt version / something became overdue after writing / the focus pick
  left the radar) -> the cached narrative is NOT shown, brief.stale=true,
  and the dashboard regenerates once (generateDailyBrief({force:true})).
  This kills the owner-caught bug: "there is no overdue item in the queue"
  printed next to a red Overdue card.
- brief-v2 prompt: queue-wide claims BANNED at the source - no counts, no
  "nothing is overdue"; name specific people/items instead. Version bumped,
  fingerprint self-heals old v1 caches on first load.
- LIVE facts line in the brief ("5 open - 1 overdue - 2 waiting on you"):
  computed from the CURRENT optimistic items at render, never from AI text;
  zeros hidden; overdue in red.
- ONE voice: the dashboard now opens with the brief's "start here" item
  already selected, so the rail's Next Best Action is about the same item -
  never two competing "do this first" recommendations.

PR 3 - AI motion layer (GSAP joins the app):
- TypeIn: the brief headline + summary write themselves in word-by-word the
  moment the AI text lands (first render instant; full text always in the
  DOM; reduced-motion plain).
- FocusThread (GSAP, lazy chunk): a glowing accent thread draws from the
  "Start here" row to that exact card on the radar - Vesta visibly points.
  Plays on hover + once per session; theme-token colors; fades on
  scroll/resize; never drawn under reduced motion.
- FLIP re-sort in TodaysRadar: when a background refresh re-ranks the same
  rows they glide to their new position (WAAPI, no dep); position map resets
  on filter/sender switch so it never stacks on the entry stagger.
- Count-up score badge: a card's number animates between values when the AI
  re-scores it (first render instant; reduced-motion snaps).

Fix: AI-rail meta line wraps instead of truncating - a long sender address
used to swallow the date ("... - Jun ..."); the time now keeps its own
non-wrapping segment.

Color pass (owner: light mode "all very white"):
- Light theme surface hierarchy rebuilt: page bg deepened to #e9f1fb, left
  sidebar is now a gray-white gradient (f6f9fd->ecf3fb) clearly distinct
  from white content cards, nested surfaces (panel-2/card) one step grayer,
  borders one step stronger so edges read on the deeper background; rail
  keeps its blue tint. Splash light base aligned.
- Chip count bubble: bg-panel -> bg-line (white-on-white disappeared in
  light mode); text-ink-soft.

Tests 482 green (brief-guard suite, TypeIn suite, live-stats + zero-hiding,
focus-default-selection); typecheck/lint/build clean. Guide updated:
daily-brief-and-focus.md.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
